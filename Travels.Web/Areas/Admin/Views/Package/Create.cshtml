@using Travels.Models.Package.DataTransferObjects
@using Travels.Utility.Enumerations
@model PackageDetailAddUpdateModel
@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Add";
    bool isGroupBooking= false;
    if (Model.PackageType == GroupPackageTypeEnum.GroupBooking)
    {
        isGroupBooking = true;
    }
}

<div class="row page-titles">
    <div class="col-md-5 col-12 align-self-center">
        <h3 class="text-themecolor mb-0">Package</h3>
        <ol class="breadcrumb mb-0 p-0 bg-transparent">
            <li class="breadcrumb-item"> <a href="/Admin">Dashboard</a> </li>
            <li class="breadcrumb-item"><a href="@Url.Action("Index","Package",new {Area="Admin"})">Package</a></li>
            <li class="breadcrumb-item active">Create</li>
        </ol>
    </div>
</div>

<br />

<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <div class="card-body">

                <form asp-action="Create" asp-controller="Package" method="post" id="createForm">
                    @Html.HiddenFor(model => model.IsPublished)
                    @Html.HiddenFor(model => model.IsActive)
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label for="inputText" class="col-sm-12 col-form-label">Tour Type</label>
                            @Html.ListBoxFor(model => model.TourTypeId, new SelectList(Model.TourTypes, "Id", "Name"), new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.TourTypeId, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-6">
                            <label for="inputText" class="col-sm-12 col-form-label">@(!isGroupBooking ? "Class Type" : "Hotel Category")</label>
                            @Html.ListBoxFor(model => model.ClassOptionId, new SelectList(Model.ClassOptionTypes, "Id", "Name"), new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.ClassOptionId, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-6">
                            <label for="inputText" class="col-sm-6 col-form-label">Type</label>
                            <select class="form-control" asp-for="PackageType" asp-items="Html.GetEnumSelectList<GroupPackageTypeEnum>()"></select>
                            @Html.ValidationMessageFor(model => model.PackageType, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-6">
                            <label for="inputText" class="col-sm-6 col-form-label">Code</label>
                            <input type="text" asp-for="Code" class="form-control" placeholder="Code">
                            @Html.ValidationMessageFor(model => model.Code, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-12">
                            <label for="inputText" class="col-sm-6 col-form-label">Name</label>
                            <input type="text" asp-for="Name" class="form-control" placeholder="Name">
                            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                <label for="inputText" class="col-sm-4 col-form-label">Country</label>
                                @Html.DropDownListFor(model => model.CountryId, new SelectList(Model.Countries, "Id", "CountryName"),
                                         new { @class = "form-control select2" })
                                @Html.ValidationMessageFor(model => model.CountryId, "", new { @class = "text-danger" })
                            </div>
                            <div class="col-md-4">
                                <label for="inputText" class="col-sm-4 col-form-label">@(isGroupBooking ? "Closed date" : "Validity")</label>
                                <input type="date" asp-for="Validity" class="form-control" placeholder="Validity">
                                @Html.ValidationMessageFor(model => model.Validity, "", new { @class = "text-danger" })
                            </div>
                            <div class="col-md-4">
                                <label for="inputText" class="col-sm-4 col-form-label">Rating</label>
                                <input type="number" asp-for="Rating" class="form-control" placeholder="Rating" step="any">
                                @Html.ValidationMessageFor(model => model.Rating, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="col-md-4">
                            <label for="inputText" class="col-sm-4 col-form-label">Day</label>
                            <input type="number" asp-for="Day" class="form-control" placeholder="fsy">
                            @Html.ValidationMessageFor(model => model.Day, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-4">
                            <label for="inputText" class="col-sm-4 col-form-label">Night</label>
                            <input type="number" asp-for="Night" class="form-control" placeholder="Night">
                            @Html.ValidationMessageFor(model => model.Night, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-4">
                            <label for="inputText" class="col-sm-6 col-form-label">@(isGroupBooking ? "Departure date" : "Start Date")</label>
                            <input type="text" asp-for="StartDate" class="form-control" placeholder="StartDate">
                            @Html.ValidationMessageFor(model => model.StartDate, "", new { @class = "text-danger" })
                        </div>
                        @* <div class="col-md-4">
                        <label for="inputText" class="col-sm-2 col-form-label">MinAge</label>
                        <input type="number" asp-for="MinAge" class="form-control" placeholder="Min Age">
                        @Html.ValidationMessageFor(model => model.MinAge, "", new { @class = "text-danger" })
                        </div> *@
                        <div class="col-md-6">
                            <label for="inputText" class="col-sm-6 col-form-label">Banner Image</label>
                            <input type="file" asp-for="PackageImg" class="form-control" />
                        </div>
                        <div class="col-md-12">
                            <label for="inputText" class="col-sm-12 col-form-label">Package Summary</label>
                            <textarea class="form-control" asp-for="PackageSummary"> </textarea>
                            @Html.ValidationMessageFor(model => model.PackageSummary, "", new { @class = "text-danger" })
                        </div>

                        <div class="col-md-12">
                            <label for="inputText" class="col-sm-12 col-form-label">Terms And Condition</label>
                            <textarea class="form-control" asp-for="TermsAndCondition"> </textarea>
                            @Html.ValidationMessageFor(model => model.TermsAndCondition, "", new { @class = "text-danger" })
                        </div>

                    </div>

                    <div class="pt-3">
                        <button type="submit" class="btn btn-primary">Submit</button>
                        <button type="button" class="btn btn-secondary" onclick="location.href='@Url.Action("Index", "Package", new { Area = "Admin" })'">Back</button>
                    </div>
                </form>

            </div>
        </div>

    </div>
</div>
@section Scripts {
    <script src="~/pagescripts/admin/package/createpackage.js"></script>
    <script>
        var cp = new CreatePackage();
        cp.init();
    </script>
}