@using vHolidays.Models.TailorTrip.DatabaseModel
@model List<TailorMadeTrip>
@{
    Layout = null;
}

<div class="col-lg-12">
    <div class="row">
        <div class="col-md-12">
            <div class="table-responsive" style="margin-top:20px">
                <table class="table customize-table table-bordered mb-0 v-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Id</th>
                            <th>TravelDate</th>
                            <td>Length</td>
                            <td>PAX</td>
                            <td>Actions</td>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model is not null && Model.Count > 0)
                        {
                            foreach (var item in Model)
                            {
                                <tr>

                                    <td>@item.Id </td>
                                    <td>@item.TravelDate </td>
                                    <td>@item.Length </td>
                                    <td>
                                        <span>@item.Adult ADT</span>
                                        <span>@item.Child CHD</span>
                                        <span>@item.Infant INF</span>
                                    </td>
                                    <td>
                                        <a href="#" class="link" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                                            <i class="fa-solid fa-ellipsis"></i>
                                        </a>
                                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                            <li>
                                                <a class="dropdown-item btn-delete" href="@Url.Action("Details","TailorTrip",new{ @area="Admin", Id=item.Id})" data-id="@item.Id">
                                                    <i class="fa-solid fa-circle-info"></i> Detail
                                                </a>
                                            </li>
                                        </ul>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="6">
                                    <div class="text-center">
                                        <h5 class="card-title">No Vendors Found</h5>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

